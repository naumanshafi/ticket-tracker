{
  "name": "tooling-ticket-tracker",
  "version": "1.0.0",
  "description": "A modern, full-featured ticket tracking and project management application",
  "author": "Nauman Shafi",
  "license": "MIT",
  "keywords": ["ticket-tracker", "project-management", "react", "fastapi", "kanban", "issue-tracking", "tooling"],
  "repository": {
    "type": "git",
    "url": "https://github.com/naumanshafi/tooling-ticket-tracker.git"
  },
  "scripts": {
    "install-all": "cd client && npm install && cd ../api && pip install -r requirements.txt",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:client\"",
    "dev:api": "cd api && source venv/bin/activate && python main.py",
    "dev:client": "cd client && npm start",
    "build": "cd client && npm run build",
    "test": "cd client && npm test"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=14.0.0",
    "python": ">=3.8.0"
  }
}
